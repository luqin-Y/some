# 校长在线


## 脚手架 yo
 + 全局安装 yo generator-webapp
 + npm i
 + gulp serve




```
Command line instructions


Git global setup

git config --global user.name "姚鲁秦"
git config --global user.email "yaoluqin@gaosiedu.com"

Create a new repository

git clone ssh://git@172.16.12.100:30022/fe/headmaster.git
cd headmaster
touch README.md
git add README.md
git commit -m "add README"
git push -u origin master

Existing folder

cd existing_folder
git init
git remote add origin ssh://git@172.16.12.100:30022/fe/headmaster.git
git add .
git commit -m "Initial commit"
git push -u origin master

Existing Git repository

cd existing_repo
git remote add origin ssh://git@172.16.12.100:30022/fe/headmaster.git
git push -u origin --all
git push -u origin --tags
```
